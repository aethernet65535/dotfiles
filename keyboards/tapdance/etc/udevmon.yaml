# /etc/interception/udevmon.yaml

- JOB: "intercept -g $DEVNODE | dual-function-keys -c /home/user/.config/keyboard/dual-function-keys.yaml | uinput -d $DEVNODE"
  DEVICE:
    EVENTS:
      EV_KEY: [KEY_LEFTCTRL]
